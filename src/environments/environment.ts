const keycloakConfig = {
  url: 'https://localhost/auth',
  realm: 'real',
  clientId: 'cliId',
};

export const environment = {
  apiUrl: "http://localhost:8080/api/",
  production: false,
  keycloakConfig
  
};
